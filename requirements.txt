# Flask และ LINE Bot SDK
flask==2.3.3
line-bot-sdk==3.5.0

# Machine Learning และ Computer Vision
torch==2.0.1+cpu  # ใช้ CPU version เพื่อประหยัด memory
torchvision==0.15.2+cpu
ultralytics==8.0.120
opencv-python-headless==4.8.0.74  # headless version สำหรับ server

# Image Processing
Pillow==10.0.0
numpy==1.24.3

# Utilities
requests==2.31.0
python-dotenv==1.0.0  # สำหรับจัดการ environment variables ใน development

# Optional: สำหรับ production
gunicorn==21.2.0  # WSGI server ที่แนะนำสำหรับ production
